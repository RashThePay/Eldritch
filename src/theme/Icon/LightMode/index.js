import React from 'react';
import LightMode from '@theme-original/Icon/LightMode';
import { Sun } from '../../../components/Logo/UI';

export default function LightModeWrapper(props) {
  return (
    <>
      <Sun {...props} />
    </>
  );
}
