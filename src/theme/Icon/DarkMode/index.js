import React from 'react';
import DarkMode from '@theme-original/Icon/DarkMode';
import { Moon } from '../../../components/Logo/UI';

export default function DarkModeWrapper(props) {
  return (
    <>
      <Moon {...props} />
    </>
  );
}
